
document.addEventListener('DOMContentLoaded',async()=>{const d=await fetch('content.json').then(r=>r.json());const esc=s=>String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));let items=JSON.parse(localStorage.getItem('sv-board')||'null')||d.productionBoard.items;const statuses=d.productionBoard.statuses;const board=document.querySelector('[data-board]');function render(){board.innerHTML=statuses.map(st=>'<section class="column"><h3>'+st+'</h3>'+items.filter(x=>x.status===st).map(x=>'<article class="card"><small>'+esc(x.priority)+' priority</small><h4>'+esc(x.title)+'</h4><p>'+esc(x.platform)+'</p><button data-move="'+x.id+'">Move →</button></article>').join('')+'</section>').join('');board.querySelectorAll('[data-move]').forEach(b=>b.onclick=()=>{const x=items.find(i=>i.id===b.dataset.move),n=Math.min(statuses.length-1,statuses.indexOf(x.status)+1);x.status=statuses[n];localStorage.setItem('sv-board',JSON.stringify(items));render()})}render();document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));document.querySelectorAll('[data-panel]').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('[data-panel="'+b.dataset.tab+'"]').classList.add('active')});const f=document.querySelector('[data-cms]'),p=document.querySelector('[data-preview]');function prev(){const v=Object.fromEntries(new FormData(f));p.innerHTML='<img src="'+esc(v.image||'hero-cinematic.jpg')+'"><h2>'+esc(v.title||'Headline preview')+'</h2><p>'+esc(v.summary||'Summary preview')+'</p>'}f.oninput=prev;f.onsubmit=e=>{e.preventDefault();const a=JSON.parse(localStorage.getItem('sv-drafts')||'[]');a.push(Object.fromEntries(new FormData(f)));localStorage.setItem('sv-drafts',JSON.stringify(a));alert('Draft saved locally')};prev();document.querySelector('[data-sponsors]').innerHTML=d.sponsorPipeline.map(s=>'<tr><td>'+esc(s.brand)+'</td><td>'+esc(s.stage)+'</td><td>$'+Number(s.value).toLocaleString()+'</td><td>'+esc(s.nextAction)+'</td></tr>').join('');document.querySelector('[data-export]').onclick=()=>{const blob=new Blob([JSON.stringify({board:items,drafts:JSON.parse(localStorage.getItem('sv-drafts')||'[]')},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='slanted-view-workspace.json';a.click()}}); 
